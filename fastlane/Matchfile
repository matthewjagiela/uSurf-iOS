git_url("git@github.com:matthewjagiela/SigningCerts.git")
storage_mode("git")

readonly(true) # Set to false to generate new certificate and profiles
force(false) # Set to false to only generate new profiles if they don't exist

type("development")
app_identifier(["com.uapps.uSurf"])



# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match
